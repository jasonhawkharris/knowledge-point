{
  "name": "knowledge-point",
  "version": "0.1.0",
  "description": "AI agent for analyzing files with zero data retention",
  "main": "dist/main/index.js",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development concurrently \"npm run vite\" \"npm run electron-dev\" \"npm run watch:main\"",
    "vite": "vite",
    "watch:main": "npm run build:preload && tsc -p tsconfig.main.json --watch",
    "build:main": "tsc -p tsconfig.main.json",
    "build:preload": "tsc src/main/preload.ts --outDir dist/main --module commonjs --target es2020 --moduleResolution node --allowSyntheticDefaultImports",
    "electron-dev": "wait-on http://localhost:5173 && electron .",
    "build": "vite build && npm run build:preload && npm run build:main && electron-builder",
    "build:vite": "vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@openrouter/ai-sdk-provider": "^1.2.1",
    "ai": "^5.0.90"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.3",
    "@types/node": "^20.10.6",
    "@vitejs/plugin-basic-ssl": "^1.1.0",
    "concurrently": "^8.2.2",
    "electron": "^28.0.0",
    "electron-builder": "^24.6.4",
    "svelte": "^4.2.8",
    "svelte-preprocess": "^6.0.3",
    "typescript": "^5.3.3",
    "vite": "^5.0.8",
    "wait-on": "^7.2.0"
  }
}
